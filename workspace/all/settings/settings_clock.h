#ifndef SETTINGS_CLOCK_H
#define SETTINGS_CLOCK_H

#include "sdl.h"

// Run the clock adjustment screen (blocks until user confirms or cancels)
void clock_adjustment_run(SDL_Surface* screen);

#endif // SETTINGS_CLOCK_H
