#ifndef SETTINGS_INPUT_H
#define SETTINGS_INPUT_H

#include "sdl.h"

// Run the input tester screen (blocks until user presses SELECT+START)
void input_tester_run(SDL_Surface* screen);

#endif // SETTINGS_INPUT_H
